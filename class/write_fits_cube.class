! write_fits_cube

! PURPOSE

! Convert a CLASS .lmv cube into a .fits file

! SYNTAX

! @ write_fits_cube input_file output_file

! tell CLASS to be quiet
sic MESSAGE CLASS S=FE

say Converting &1 to fits file &2

! write an "init" file to use the task gildas_fits
sys "../../../class/write_fits_init.sh &1 &2"

! AKL - commented because of SICs apparent inability to print a "
! sys "rm temp.init"
! sic output temp.init
! say ! > temp.init
! say ! Gildas to Disk FITS translator
! say ! <automatically generated by write_fits_cube.class>
! say !
! say TASK\FILE "Input Gildas File" IN$ "&1" /OLD
! say TASK\FILE "Output FITS file" FITS$ "&2" /NEW
! say TASK\INTEGER "Number of bits" NBIT$ -32
! say TASK\CHARACTER "Style of FITS file (STANDARD or UVFITS)" STYLE$ "STANDARD"
! say TASK\GO
! sic output

! run the gildas fits routine using our temporary init file
sic delete &2
run gildas_fits temp.init /nowindow

